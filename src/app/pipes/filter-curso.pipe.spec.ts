import { FilterCursoPipe } from './filter-curso.pipe';

describe('FilterCursoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCursoPipe();
    expect(pipe).toBeTruthy();
  });
});
